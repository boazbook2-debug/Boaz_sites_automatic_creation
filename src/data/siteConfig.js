// Not part of the per-client rebrand contract (agency.js / agents.js / properties.js).
// This one flag controls whether sample/placeholder photos show the diagonal
// "EXAMPLE" watermark. Set to false yourself before launching a real client site.
const siteConfig = {
  showSampleWatermark: false,
};

export default siteConfig;
