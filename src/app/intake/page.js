import IntakeFlow from "@/components/intake/IntakeFlow";

export const metadata = {
  title: "שאלון הקמת אתר",
  robots: "noindex, nofollow",
};

export default function IntakePage() {
  return <IntakeFlow />;
}
